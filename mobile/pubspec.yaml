name: zionpattern
description: 0.4.0. Score 75 = intentional suppression (hard cap). 1–74 = more natural. Provisional and assistive. Does not solve cases.
publish_to: "none"
version: 0.4.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
